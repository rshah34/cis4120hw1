# Rohan Shah - HW1: HTML/CSS of Course Selection Platform

To run code, simply open `index.html` in a web browser - there, you'll see the second screen in a static implementation with a clean interface that addresses user concerns with the current structure of course selection via Path@Penn.

Implementation was exported from Figma and extended on/cleaned up with help from ChatGPT.
